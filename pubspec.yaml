name: detect_clap_sound_flutter
description: "A new Flutter plugin project."
version: 1.1.1
homepage: https://github.com/AnhPhon
repository: https://github.com/AnhPhon/detect_clap_sound_flutter/

environment:
  sdk: ^3.3.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: techmind.studio.detect_clap_sound_flutter
        pluginClass: DetectClapSoundFlutterPlugin
